// app/(auth)/signin/page.server.tsx (or similar server-side file)
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Увійти - UniverHub",
  description: "Сторінка входу для студентів та викладачів UniverHub.",
};

export default function SignIn() {
  // Server-rendered content here
  return (
    <div>
      {/* Server-rendered content */}
    </div>
  );
}
